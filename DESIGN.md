# 构形 WebCAD 设计规格

参考图：`docs/webcad-ui-concept.png`

## 视觉系统

- 背景：真白 `#ffffff`；画布 `#fbfcfe`；侧栏 `#f7f9fc`。
- 文字：主色 `#172033`；次要 `#667085`；边框 `#d9e0ea`。
- 操作色：钴蓝 `#1769e0`；通过 `#36a33c`；警告 `#e67628`。
- 圆角：控件 4px，浮层 8px；阴影仅用于菜单、提示和模态层。
- 字体：Segoe UI / Microsoft YaHei；工具栏 12px，正文 13px，区标题 14px。

## 布局与组件

- 顶部 56px：品牌、工程保存、草图/特征工具、导出和打印自检。
- 左栏 228px：草图工具、特征树；选中项用浅蓝底与蓝色竖线。
- 中央：Three.js 视口、网格、轴、视图立方体、底部相机工具。
- 右栏 300px：参数、打印自检、导出格式。
- 底栏 28px：状态、单位、选择和坐标。
- 所有交互文字和图标均为 HTML/SVG；模型本身由 WebGL 渲染。

## 核心路径

新建草图 → 画线/矩形/圆并吸附 → 填尺寸 → 拉伸/旋转/切除 → 圆角/倒角 → 回改特征参数 → 打印自检 → 导出。
